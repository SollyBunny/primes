#include "common.h"

#error "TODO: doing this rn :)"

number test(number n) {
    number primes[n];
    while (prime < n) {
        // check if n is a prime
        for (number i = 2; i < num; ++i) {
            if (num % i == 0)
                goto l_fail;
        }
        prime += 1;
        l_fail:
        num += 1;
    }
    return primes[n - 1];
}